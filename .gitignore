# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual envs
venv/
env/
.venv/
.env

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Claude Code (config local de la herramienta)
.claude/
# Excepción: slash commands del proyecto (parte del agente v0.7.2 — versionables)
!.claude/commands/
!.claude/commands/**

# Backups locales (no versionar)
*.bak.*

# Ruflo / claude-flow runtime (defensivo — NO debería existir per DT-008).
# Si accidentalmente reaparece, estas entradas evitan que contamine git.
.claude-flow/
.swarm/
ruvector.db
.mcp.json
daemon.pid

# Project-specific
reportes_generados/
*.sqlite
*.sqlite-journal

# Notas locales (no versionar — viven solo en el repo del owner)
MENSAJE_INICIAL_CODE.md

# Pero NO ignorar los L5X — son la fuente de verdad
# parque_l5x/*.L5X      ← NO descomentar; los L5X SÍ se versionan

# Excepción: L5X grandes / con IP de terceros NO se versionan
# (decisión operativa 2026-05-03 — son ~15 MB cada uno; el paquete los
# procesa correctamente vía load_project pero no los necesita en repo)
parque_l5x/CPPIM_BD800_1.L5X
parque_l5x/CPPIM_BD800_1_010325_paro53min.L5X
docs/Inf Fase 3/*.L5X

# OS
Thumbs.db
.DS_Store
